# Dependencies
node_modules/

# Environment variables
.env
.env.*
!.env.*.example

# Next.js
app/.next/
app/tsconfig.tsbuildinfo

# CLI build output is committed so `npm install -g` from GitHub works
# without a build step. Rebuild with: cd cli && npm run build

# Pagefind (generated search index, rebuilt at deploy)
app/public/pagefind/

# Test artifacts
app/test-results/
app/playwright-report/
app/audit-shots/

# Terraform
infra/.terraform/
infra/terraform.tfstate
infra/terraform.tfstate.backup
infra/*.tfplan

# Lambda build artifacts (zips are built from lambda/src, not committed)
infra/lambda-src/*.zip

# macOS
.DS_Store

# Claude auto-memory
memory/
